# TASK REPORT

## METADATA
- Date: 04-09-26 (DD-MM-YY)
- Time: 16:58 (America/Sao_Paulo, -03:00)
- Branch: docs/rules00-skills-vs-rules-note (criada a partir de main@d9e4afa)
- Task: Registrar em `rules/00-operating-model.md` por que `rules/` fica fora de `.claude/skills/` (pesquisa de boas práticas oficiais da Anthropic para Agent Skills); registrar diretiva permanente em auto-memory sobre seguir essas diretrizes ao criar novas skills
- Result: SUCCESS (nota registrada; aguardando Gate 1)

## SUMMARY
- Pedido do usuário: avaliar se as regras deveriam estar dentro das skills ou
  se o formato atual (regra canônica separada, skill enxuta que a cita como
  autoridade) é melhor, segundo as boas práticas oficiais da Anthropic.
- Pesquisa via agente `claude-code-guide` contra fontes oficiais (Claude Code
  docs, Agent Skills specification, Claude Platform docs). Achados: (a)
  progressive disclosure e o limite de ~500 linhas por `SKILL.md` já são
  cumpridos pelo design atual (maior skill do projeto: `git-closure`, 225
  linhas); (b) a convenção oficial de "bundled resources dentro da pasta da
  skill" existe para material que pertence a uma skill específica ou para
  portabilidade de skill como plugin — nenhuma das duas se aplica às regras
  deste projeto (rules/12 e 13 não têm skill correspondente; rules/14 §12 é
  citada por 2 skills diferentes; nenhuma skill é portável para outro repo).
- Veredito registrado: manter `rules/` como fonte única de política; mover
  regras para dentro de pastas de skill duplicaria política canônica —
  exatamente o que as Regras 12 e 13 existem para evitar.
- Nota adicionada a `rules/00-operating-model.md` (seção nova, "Por que
  `rules/` fica fora de `.claude/skills/`"), para não reabrir a análise do
  zero numa próxima sessão.
- Diretiva permanente registrada em auto-memory (fora do repo,
  `~/.claude/.../memory/skills-follow-official-anthropic-guidelines.md`): ao
  criar novas skills neste projeto, seguir as diretrizes oficiais de Agent
  Skills (progressive disclosure, `SKILL.md` enxuto, bundled resources
  específicos da skill) — com a mesma exceção de não duplicar `rules/`.
- **Incidente repetido:** a edição de `rules/00-operating-model.md` foi feita,
  de novo, com `main` checked-out, sem branch — a mesma falha que motivou o
  pré-voo da skill `cc-flow` criada nesta própria sessão, e que eu não apliquei
  a mim mesmo desta vez. Corrigido antes do commit com `git checkout -b
  docs/rules00-skills-vs-rules-note`; nada chegou a ser commitado em `main`.

## FILES
### Created (nesta tarefa)
- `reports/report_task_04-09-26_16-58.md` — este relatório.
- `~/.claude/.../memory/skills-follow-official-anthropic-guidelines.md` —
  fora do repo, não versionado; diretiva permanente de auto-memory.

### Modified (nesta tarefa)
- `rules/00-operating-model.md` — nova seção "Por que `rules/` fica fora de
  `.claude/skills/`"; linha final atualizada para incluir `cc-flow` na lista
  de skills executáveis.

### Deleted / Moved
- Nenhum.

## VALIDATION
- `git diff --stat` → `rules/00-operating-model.md` `+25 −1`.
- Conferido: nenhum `SKILL.md` do projeto passa de 225 linhas (`wc -l
  .claude/skills/*/SKILL.md`), evidência citada na nota.
- Recursão de relatório: nenhuma.

## GIT
- status: branch `docs/rules00-skills-vs-rules-note` a partir de
  `main@d9e4afa`; `rules/00-operating-model.md` modificado + este relatório
  untracked; nada staged.
- diff summary: `rules/00-operating-model.md` `+25 −1`.
- commit: not executed — aguardando Gate 1.
- push: not executed — aguardando Gate 2.
- PR: not created — aguardando autorização separada; base pretendida `main`.

## WARNINGS / CONCURRENT CHANGES
- Nenhuma alteração concorrente.
- **Incidente registrado (2ª ocorrência na mesma sessão):** edição iniciada em
  `main` sem branch, corrigida antes do commit. A skill `cc-flow` (PR #24,
  já mergeada) tem exatamente este pré-voo como passo 2 do seu procedimento —
  não foi invocada/aplicada desta vez antes de editar. Ação corretiva: nenhuma
  mudança de skill necessária (o procedimento já existe e está correto); o
  gap é de execução (esquecer de segui-lo), não de design. Registrado para
  reforçar o hábito.

## PENDING
- Gate 1 (Regra 14): `rules/00-operating-model.md` + este relatório, 1 commit
  (escopo único).
  - Mensagem proposta: `docs(rules): registra por que rules/ fica fora de .claude/skills/`
- Gate 2 (push) e PR (base `main`) — autorizações separadas.
- Checkpoint 4 (Fronteira): após o merge desta PR e o Gate 4 correspondente,
  parar e pedir o próximo passo (M008 ou M004, per NEXT da memory).

## PEDAGOGICAL CHECKPOINT SUMMARY

### Checkpoint 0 — Abordagem
- Pergunta do usuário já respondida na análise anterior (regras fora vs.
  dentro das skills) — sem novas opções técnicas a comparar aqui, só registro.

### Checkpoint 1 — Plano
- Implícito no pedido direto do usuário ("registra a nota... mantenha isso na
  memória") — plano simples e de baixo risco (1 seção nova em 1 arquivo +
  1 memory fora do repo), executado sem Checkpoint 1 formal separado.

### Checkpoint 2 — Execução
- Resultado: nota registrada em `rules/00`; memory permanente registrada.
- Divergência: incidente de branch (ver WARNINGS), corrigido antes do commit.

### Checkpoint 3 — Sync & Cleanup
- Não aplicável ainda (sem merge desta unidade).

### Checkpoint 4 — Fronteira / Encerramento
- Pendente: após Gate 1/2/PR/Gate 3/Gate 4 desta PR, parar e pedir o próximo passo.

### Aprendizados registrados
- Ter acabado de criar uma skill para resolver um problema não é o mesmo que
  segui-la — o pré-voo da `cc-flow` só previne o incidente se for de fato
  invocado/lembrado antes de editar, não só por existir no repositório. Vale
  reforçar isso como hábito consciente, não assumir que o artefato sozinho
  resolve.

### Próximo passo
- Gate 1 desta PR; depois parar (Checkpoint 4) e apresentar NEXT (M008 ou M004).

## NEXT
- Apresentar Checkpoint 2 (validação) e, após confirmação, o Gate 1.
